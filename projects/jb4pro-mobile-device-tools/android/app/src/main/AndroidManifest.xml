<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jb4pro">
    
    <!-- ========= PERMISSIONS ========= -->
    <!-- (1) Legacy Bluetooth permissions used on older Android (below 12). -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- (2) New Android 12+ Bluetooth permissions. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Only add this if your app does BLE advertising:
         <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" /> -->

    <!-- (3) Location permissions required on older devices to scan for BLE. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- (4) Optional: read/write storage if you truly need file IO on device storage. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- (5) For network calls, firmware updates, etc. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- If BLE is critical to your app and you want to restrict unsupported devices, set required="true". 
         If you want to allow install on devices without BLE, use required="false". -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- ========= APPLICATION ========= -->
    <application
        android:label="jb4pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"> <!-- For Android 10 storage compatibility -->

        <!-- Your MainActivity setup -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- This meta-data is required by newer Flutter versions (Embedding v2). -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- ========= QUERIES SECTION (for Android 11+ package visibility) ========= -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

</manifest>
