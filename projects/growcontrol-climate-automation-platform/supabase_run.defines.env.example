# Copy to `supabase_run.defines.env` and fill in for your project. The anon
# (publishable) key is safe to ship inside the app; the service-role key is
# NOT and must never live here.
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_ANON_KEY=sb_publishable_REPLACE_ME
