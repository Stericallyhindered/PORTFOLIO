// Growmie Hub — ESP32 grow-room controller.
//
// The real setup() and loop() live in src/main.cpp. Arduino IDE compiles
// every .cpp under src/ automatically, so this .ino stays minimal.
//
// Required libraries (Library Manager):
//   ArduinoJson (Benoit Blanchon)   7.x
//   WiFiManager (tzapu)              2.x
//
// Board:
//   Tools → Board → ESP32 Arduino → your specific ESP32 module
